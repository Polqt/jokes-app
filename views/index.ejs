<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="styles/styles.css">

        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
                font-family: 'Figtree', sans-serif;
                color: white;
            }

            body {
                height: 100vh;
                width: 100%;
                background-color: #001F3F;
                position: relative;
            }

            header {
                font-size: 1.5rem;
                text-align: center;
                padding: 2rem;
            }

            .container {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                display: flex;
                flex-direction: column;
                flex-grow: 1;
                gap: 2rem;
            }

            #jokes {
                border: 2px solid white;
                border-radius: 1rem;
                width: 600px;
                height: 150px;
                font-size: 1.4rem;
                padding: 1rem;
            }

            #setup {
                margin-bottom: 1rem;
            }

            button {
                border: none;
                outline: none;
                background-color: #3A6D8C;
                color: white;
                padding: 1rem 2rem;
                border-radius: 1rem;
                font-size: 1rem;
                transition: background-color 0.3s ease, transform 0.3s ease; /* Added transition */
            }

            button:hover {
                background-color: #4B4B4B;
                color: white;
                transform: scale(1.05); 
            }

            .description {
                display: flex;
                justify-content: space-between;
            }
        </style>
    </head>
    <body>
        <header>
            <h1>Joke Generator</h1>
        </header>

        <div class="container">
            <div id="jokes">
                <% if (setup) { %>
                    <p id="setup"><%= setup %></p>
                    <p><%= punchline %></p>
                <% } else { %>
                    <p> Click the button to generate a joke!</p>
                <% } %>
            </div>     
            <div class="description">
                <% if (type && id) { %>
                    <p>Type: <%= type %> </p>
                    <p>ID: <%= id %> </p>
                <% } %>
            </div>   
            <button onclick="window.location.href='/jokes'">Generate Joke</button>
        </div>
    </body>
</html>
